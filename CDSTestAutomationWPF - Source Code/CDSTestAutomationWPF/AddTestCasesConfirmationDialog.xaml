<Window x:Class="CDSTestAutomationWPF.AddTestCasesConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Add Test Cases to TFS Suite" Height="420" Width="580"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Background="#FFE5E5E5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message Content Area -->
        <ScrollViewer Grid.Row="0" Margin="20" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageTextBlock"
                       TextWrapping="Wrap"
                       FontFamily="Consolas"
                       FontSize="11"
                       Foreground="#333333"
                       LineHeight="18"
                       Padding="10"
                       Background="White"
                       BorderBrush="#CCCCCC"
                       BorderThickness="1"/>
        </ScrollViewer>

        <!-- Button Area -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20" Height="40">
            <Button x:Name="YesButton"
                    Content="YES - Add Test Cases"
                    Click="YesButton_Click"
                    Width="140"
                    Height="35"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="Bold"
                    Margin="0,0,10,0"
                    Cursor="Hand"/>
            <Button x:Name="NoButton"
                    Content="NO - Cancel"
                    Click="NoButton_Click"
                    Width="100"
                    Height="35"
                    Background="#f44336"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
